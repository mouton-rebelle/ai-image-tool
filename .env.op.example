# Copy this file to .env.op and replace each URI with its 1Password secret reference.
# `op item get <item> --format=json` exposes field references without printing them.
XAI_API_KEY="op://<vault>/<xai-item>/<field>"
CIVITAI_TOKEN="op://<vault>/<civitai-item>/<field>"
